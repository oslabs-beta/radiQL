version: "3.0"
services:
  dev: 
    image: rjiang12/radiql-prod
    container_name: radiql-dev-hot
    ports:
      - "8080:8080"
    volumes:
      - .:/usr/src/app
      - node_modules:/usr/src/app/node_modules
    command: npm run dev:hot
volumes:
  node_modules:
  dev-db-volume: 